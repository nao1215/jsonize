description: the uptime line joined after the counters is refused rather than read
source: the definition's own fixture followed by the uptime/bsd fixture, joined for this case
os: darwin
args: []
expect_error: does not match pattern
