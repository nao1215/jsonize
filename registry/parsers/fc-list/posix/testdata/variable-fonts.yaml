description: A family shipped as both named styles and variable fonts, where the variable files name no style at all.
source: the Ubuntu font lines of `fc-list | sort`, captured on Ubuntu 26.04 (fontconfig 2.15) with LC_ALL=C
os: linux
