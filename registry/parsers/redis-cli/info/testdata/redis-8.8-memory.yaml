source: captured from Redis 8.8.0 on Linux with LC_ALL=C from a server started for the capture on port 16379 (redis-cli -p 16379 info memory); CRLF line endings as redis-cli wrote them
description: One section named on the command line, so the list holds one record.
os: linux
args: ["-p", "16379", "info", "memory"]
