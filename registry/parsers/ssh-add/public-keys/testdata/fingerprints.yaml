description: The output of ssh-add -l, fingerprints rather than keys, which belongs to ssh-keygen/fingerprint under its ssh-add alias.
source: captured on Ubuntu 26.04 with OpenSSH 10.2p1, LC_ALL=C ssh-add -l against a throwaway agent holding keys made for the fixture
os: linux
args: [-l]
expect_error: 'does not describe this input'
