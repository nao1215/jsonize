description: Only -subject and -issuer.
source: captured on Ubuntu 26.04 with OpenSSL 3.5.5, LC_ALL=C openssl x509 -noout -subject -issuer over a certificate made for the fixture
os: linux
args: [x509, -in, c.pem, -noout, -subject, -issuer]
