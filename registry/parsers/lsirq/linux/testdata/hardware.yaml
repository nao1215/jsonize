description: The first rows of the hardware interrupt counts, including the lettered lines the kernel counts on its own.
source: the header and rows 1 to 11 of `lsirq` captured on Ubuntu 26.04 (util-linux 2.41) with LC_ALL=C
os: linux
args: []
