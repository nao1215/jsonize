description: Rows from the middle of the listing, including the counters the kernel keeps without a name.
source: the header and rows 83 to 88 of `lsirq` captured on Ubuntu 26.04 (util-linux 2.41) with LC_ALL=C
os: linux
