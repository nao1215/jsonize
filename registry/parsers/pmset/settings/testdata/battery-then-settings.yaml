description: the output of pmset -g batt in front of the settings is refused, since the text no longer opens with the settings heading
source: the pmset/battery fixture followed by this definition's own fixture, both captured on GitHub Actions macos-latest (macOS 26.6.2), joined for this case
expect_error: does not describe this input
