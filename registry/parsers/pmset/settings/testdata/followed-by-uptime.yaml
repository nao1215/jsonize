description: the uptime line joined after the settings is refused rather than read as a setting
source: the definition's own fixture followed by the uptime/bsd fixture, joined for this case
os: darwin
args: [-g]
expect_error: does not describe this input
