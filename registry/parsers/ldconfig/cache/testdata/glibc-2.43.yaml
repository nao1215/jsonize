source: >-
  captured on Ubuntu 26.04 (GNU C Library 2.43) with LC_ALL=C as
  `ldconfig -p`; the output has 1909 lines and this is lines 1-25 (the
  count line and the first entries) followed by line 1909, the trailing
  "Cache generated by" line. Nothing was replaced; the entries name only
  system paths.
os: linux
args: ["-p"]
