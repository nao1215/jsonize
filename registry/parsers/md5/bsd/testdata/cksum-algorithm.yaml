description: cksum -a sha256 prints the BSD line without --tag, since tagged is its default for a digest algorithm.
source: captured on Ubuntu 26.04 with LC_ALL=C over files made for the fixture, uutils coreutils 0.8.0 cksum -a sha256
os: linux
args: [-a, sha256, notes.txt, "release notes.md"]
