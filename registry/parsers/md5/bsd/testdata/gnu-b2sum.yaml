description: >
  b2sum --tag over the same four files, reached under the b2sum alias:
  BLAKE2b in front, with its lower-case letter, and a 128 digit digest.
source: captured on Ubuntu 26.04 with GNU coreutils 9.7 (gnub2sum), LC_ALL=C b2sum --tag over files made for the fixture
os: linux
args: [--tag, "back\\slash.txt", "new\nline.txt", plain.txt, "with space.txt"]
