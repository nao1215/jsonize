description: >
  sha256sum --tag over the same four files, reached under the sha256sum
  alias: SHA256 in front and a 64 digit digest.
source: captured on Ubuntu 26.04 with GNU coreutils 9.7 (gnusha256sum), LC_ALL=C sha256sum --tag over files made for the fixture
os: linux
args: [--tag, "back\\slash.txt", "new\nline.txt", plain.txt, "with space.txt"]
