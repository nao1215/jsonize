description: A link named "link -> odd" that points at plain. The line also reads as a link named link that points at "odd -> plain", so it is refused.
source: captured on Ubuntu 26.04 with BusyBox v1.37.0, LC_ALL=C busybox tar -tvf over an archive made for the fixture; the user name replaced
os: linux
args: [-tvf]
expect_error: does not have the shape
