description: Every task with memory activity over one sampling interval.
source: captured on Ubuntu 26.04 (sysstat 12.7) with LC_ALL=C from `pidstat -r 1 1`; the host name in the banner was replaced
os: linux
args: ["-r", "1", "1"]
