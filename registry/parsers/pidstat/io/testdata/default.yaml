description: the report since boot, the first ten tasks
source: captured on Ubuntu 26.04 (sysstat 12.7) with LC_ALL=C from `pidstat -d`; the host name in the banner was replaced
os: linux
args: ["-d"]
