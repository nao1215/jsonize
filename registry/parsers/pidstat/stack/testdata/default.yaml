description: the report since boot, the first eight tasks whose stack pidstat could read
source: captured on Ubuntu 26.04 (sysstat 12.7) with LC_ALL=C from `pidstat -s -p ALL 1 1`; the host name in the banner was replaced and the tasks after the eighth were dropped
os: linux
args: ["-s"]
