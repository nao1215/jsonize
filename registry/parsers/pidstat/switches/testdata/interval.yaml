description: two samples and the summary, a few tasks of each kept, one of them a kernel thread whose name has a space
source: captured on Ubuntu 26.04 (sysstat 12.7) with LC_ALL=C from `pidstat -w 1 2`, most rows of each sample left out; the host name in the banner was replaced
os: linux
args: ["-w", "1", "2"]
