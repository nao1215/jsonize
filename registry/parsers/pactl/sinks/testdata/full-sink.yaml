source: "pactl list sinks on Ubuntu 26.04 with PipeWire 1.6.2, LC_ALL=C, trimmed to the second of three sinks"
description: >-
  One sink with every nested list pactl prints: the property list, three
  ports and a format. The descriptions are in the language the audio
  server runs in, which LC_ALL=C on the client does not change.
os: linux
args: ["list", "sinks"]
