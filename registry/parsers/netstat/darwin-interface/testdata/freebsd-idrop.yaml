description: FreeBSD's table has an Idrop column and belongs to netstat/freebsd-interface, so it is refused here
source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C netstat -in, a copy of the netstat/freebsd-interface fixture numeric
expect_error: does not describe this input
