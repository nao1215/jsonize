description: >-
  netstat -e run for real on Windows Server 2022, the same banner,
  header and rows as on 2025 with counts of other widths.
source: >-
  captured on GitHub Actions windows-2022 (Windows Server 2022, build
  20348.5499) on 2026-09-15 with `cmd /c "netstat -e 2>&1"` from
  PowerShell, written with WriteAllLines (CRLF line endings). Nothing
  was replaced: the table names no host or address.
os: windows
args: ["-e"]
