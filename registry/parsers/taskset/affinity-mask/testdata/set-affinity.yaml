source: "captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C: taskset -c 0-3 sh -c 'taskset -p 5 $$', so that only the capturing shell was affected"
os: linux
description: "Setting an affinity prints the mask before and after the change. The arguments are not recorded because running them changes the affinity of the pid they name."
