description: a full name with a space, a login longer than eight characters with an empty name, and a name cut at nineteen characters
source: captured on Ubuntu 26.04 with LC_ALL=C pinky (uutils coreutils 0.8.0) inside a user and mount namespace, over a utmp file and password entries written for the capture; the text is the command's own
