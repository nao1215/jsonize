description: Two services named on the command line, each under its own heading.
source: "captured with `busctl tree org.freedesktop.login1 org.freedesktop.timesync1` (systemd 259) on Ubuntu 26.04 with LC_ALL=C"
os: linux
args: [tree, org.freedesktop.login1, org.freedesktop.timesync1]
