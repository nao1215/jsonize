source: the definition's own three-tasks.txt followed by ps/unix's procps-4.0.4.txt, joined for this case
description: a ps table after the list is refused, since its lines are not task lines
expect_error: does not describe this input
