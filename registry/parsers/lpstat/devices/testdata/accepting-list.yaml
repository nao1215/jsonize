description: lpstat -a lists the same destinations in another format, which this definition refuses
source: captured on Ubuntu 26.04 (CUPS 2.4.16) with LC_ALL=C lpstat -h HOST -a from a private cupsd started as an unprivileged user on 127.0.0.1 with its own configuration directory; the printer names and device URIs were chosen for the capture
expect_error: 'does not describe this input'
