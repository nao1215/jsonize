source: the definition's own parse-error.txt followed by df/gnu's coreutils-9.7.txt, joined for this case
description: a df table after the findings is refused, since its lines carry no SC code
expect_error: does not describe this input
