source: captured from ShellCheck 0.11.0 on Linux with LC_ALL=C, shellcheck -f gcc on a script with an unterminated string
description: A script shellcheck cannot parse, reported with SC1xxx codes and no further checks.
os: linux
args: ["-f", "gcc", "broken.sh"]
