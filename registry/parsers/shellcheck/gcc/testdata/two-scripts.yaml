source: captured from ShellCheck 0.11.0 on Linux with LC_ALL=C, shellcheck -f gcc on two scripts written for the capture
description: >-
  Eighteen findings of all three severities in two scripts, one of them
  in a directory whose name holds a space.
os: linux
args: ["-f", "gcc", "script.sh", "my tools/deploy.bash"]
