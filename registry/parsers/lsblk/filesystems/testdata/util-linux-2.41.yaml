description: the first rows of a full listing, where the loop devices carry a filesystem but no label and no UUID
source: captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C, first 10 lines
os: linux
args: [-f]
