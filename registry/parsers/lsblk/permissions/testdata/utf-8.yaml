description: in a UTF-8 locale the tree is drawn with box-drawing characters, which are not part of the name
source: captured on Ubuntu 26.04 (util-linux 2.41.3) as lsblk -m /dev/nvme0n1 /dev/nvme1n1 with LC_ALL=C.UTF-8; nothing replaced, as -m prints no UUID and no mount point
os: linux
args: [-m, /dev/nvme0n1, /dev/nvme1n1]
