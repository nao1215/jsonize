description: the first rows of a full listing, where every loop device is owned by root and carries the same mode
source: captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C, first 10 lines; nothing replaced, as -m prints no UUID and no mount point
os: linux
args: [-m]
