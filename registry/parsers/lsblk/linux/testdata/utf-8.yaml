source: captured on Ubuntu 26.04 (util-linux 2.41.3) as lsblk /dev/nvme1n1 /dev/nvme0n1 with LC_ALL=C.UTF-8; the user name in the mount point replaced
description: in a UTF-8 locale the tree is drawn with box-drawing characters, which are not part of the name
os: linux
args: [/dev/nvme1n1, /dev/nvme0n1]
