description: One dependency is declared but not installed, so its current version is MISSING and its location is "-".
source: "`npm outdated` with npm 10.9.4 (node 22.22.1) on Ubuntu 26.04 with LC_ALL=C, in a project whose node_modules is out of date"
os: linux
args: ["outdated"]
