description: one device, so the output is a single block with a trailing blank line
source: captured on Ubuntu 26.04 (pciutils 3.14.0) with LC_ALL=C, lspci -vmm -s 00:01.0
os: linux
args: [-vmm, -s, "00:01.0"]
