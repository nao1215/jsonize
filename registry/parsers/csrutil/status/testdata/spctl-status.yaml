description: the one line spctl --status prints belongs to spctl/status and is refused here
source: captured on GitHub Actions macos-latest (macOS 26.6.2) with LC_ALL=C spctl --status
expect_error: does not describe this input
