source: captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C
description: the same summary with --bytes, where two limits are printed as 20 digit numbers that no integer field could hold
os: linux
args: [-b]
