source: the definition's own default-checks.txt followed by df/gnu's coreutils-9.7.txt, joined for this case
description: a df table after the problems is refused, since its lines carry no check code
expect_error: does not describe this input
