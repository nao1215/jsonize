description: The checksum file itself, digest lines rather than verdicts, which belongs to sha256sum/posix.
source: captured on Ubuntu 26.04 with GNU coreutils 9.7 (gnusha256sum), LC_ALL=C sha256sum over files made for the fixture
os: linux
args: [hello.txt, repeat.txt]
expect_error: 'does not describe this input'
