description: A file that changed since the checksums were written (FAILED) and one that was deleted (FAILED open or read), among ones that match; the tool exited 1 and wrote its two WARNING counts to standard error. Reached under the sha256sum alias.
source: captured on Ubuntu 26.04 with GNU coreutils 9.7 (gnusha256sum), LC_ALL=C sha256sum -c over a checksum file made for the fixture
os: linux
args: [-c, gnu.sha256]
