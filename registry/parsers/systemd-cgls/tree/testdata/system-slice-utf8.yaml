source: captured with LC_ALL=C.UTF-8 systemd-cgls --no-pager /system.slice on Ubuntu 26.04 (systemd 259), the tree drawn with box-drawing characters, with the host name replaced by examplehost
os: linux
args: ["--no-pager", "/system.slice"]
