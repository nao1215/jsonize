source: captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C, the first 12 rows
description: read-only loop devices backed by snap images, with the sizelimit, offset and dio flags at zero
os: linux
