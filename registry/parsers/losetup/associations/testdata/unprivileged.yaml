source: captured on Ubuntu 26.04 (util-linux 2.41.3) as losetup -a with LC_ALL=C by an unprivileged user, the first four lines
description: without the right to ask for them the brackets are empty and there is no inode, which are null
os: linux
args: [-a]
