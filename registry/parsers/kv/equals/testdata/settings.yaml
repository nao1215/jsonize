source: "a hand-written example of the shape this definition describes; the keys carry a hyphen so the text is neither an environment listing nor a git configuration, both of which the registry already reads"
