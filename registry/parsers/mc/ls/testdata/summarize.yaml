source: captured from the same mc on Linux with LC_ALL=C and TZ Asia/Tokyo, mc ls --summarize on the same bucket
description: The blank line and the two totals --summarize adds are refused.
expect_error: does not describe this input
