source: the definition's own bucket.txt followed by etc/os-release's ubuntu-26.04.txt, joined for this case
description: os-release lines after the listing are refused, since they have no bracketed time
expect_error: does not describe this input
