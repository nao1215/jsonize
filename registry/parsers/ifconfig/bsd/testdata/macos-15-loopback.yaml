description: ifconfig lo0 on macOS 15 on Intel, one block with two IPv4 addresses and the IPv6 lines between them.
source: captured on GitHub Actions macos-15-intel (macOS 15.7.9) with LC_ALL=C and `ifconfig lo0`
os: darwin
args: ["lo0"]
