description: ifconfig -l prints the interface names on one line, which is not this format.
source: captured in the same FreeBSD 15.1 virtual machine with LC_ALL=C and `ifconfig -l`
expect_error: 'does not describe this input'
