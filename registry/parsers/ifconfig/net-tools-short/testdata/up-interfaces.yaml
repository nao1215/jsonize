description: ifconfig -s, the interfaces that are up.
source: captured with LC_ALL=C and `ifconfig -s` from net-tools 2.10 in a debian:stable-slim container with a dummy interface that is up, a second one that is down with MTU 9000, eth0 and lo
os: linux
args: ["-s"]
