source: "journalctl -k -n 5 -o short-iso --no-pager on Ubuntu with systemd 259, LC_ALL=C; host name replaced. The kernel entries carry no pid, so that field is null on three rows"
os: linux
args: ["-k", "-n", "5", "-o", "short-iso"]
