source: git 2.53 in repositories built for this fixture on Ubuntu, LC_ALL=C
description: the main worktree, a detached one, one on a branch and a locked one with a reason
args: [worktree, list, --porcelain]
