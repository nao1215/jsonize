source: git 2.53 in the jsonize repository, `git -c log.decorate=short log --oneline -2 v0.1.0` with LC_ALL=C
description: refused on a pipe, since a tag decoration opens the first subject
expect_error: does not describe this input
