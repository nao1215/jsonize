source: git 2.53 in the jsonize repository, `git -c log.decorate=short log --oneline -3` with LC_ALL=C, as a user whose configuration sets log.decorate prints it on a pipe
description: refused on a pipe, since the first subject would carry the branch names
expect_error: does not describe this input
