description: a key written with no value has no = and a null value
source: 'git config --list --show-origin --file flag.cfg, git 2.53 with LC_ALL=C in a repository made for this fixture, with a home directory and a global configuration made for it (the people are not real); the home directory is written as /home/user'
args: [config, --list, --show-origin, --file, flag.cfg]
