description: A commit that adds a binary file, which prints the two sizes instead of a line count, renames files, and has one path long enough that git elides its head. The bars are scaled to the 80 column width a pipe gets.
source: "`git diff --stat` between two commits of this repository with git 2.53.0 on Ubuntu 26.04 with LC_ALL=C"
os: linux
