description: git log --name-only prints the paths alone and no closing line, which is not this format.
source: captured with LC_ALL=C and `git log --name-only -2` from a repository made for this fixture with git 2.53 (the script sets fixed author and committer names and dates; the people are not real)
expect_error: 'does not describe this input'
