description: The same summary with -e, which puts the address in angle brackets after the name. The author names and addresses were replaced.
source: "`git shortlog -sne HEAD` with git 2.51.0 on Ubuntu 26.04 with LC_ALL=C, inside this repository"
os: linux
args: ["shortlog", "-sne", "HEAD"]
