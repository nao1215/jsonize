description: Two builders, the current one marked with an asterisk, each with the node it runs on.
source: "captured with `docker buildx ls` (buildx v0.31.1) on Ubuntu 26.04 with LC_ALL=C; the builder atago-local-smoke renamed example-builder, padded to keep the columns"
os: linux
args: [buildx, ls]
