description: >-
  ver run for real on Windows Server 2025, the version line with a
  five-digit build and revision.
source: >-
  captured on GitHub Actions windows-latest (Windows Server 2025, build
  26100.33296) on 2026-09-15 with `cmd /c "ver 2>&1"` from PowerShell,
  written with WriteAllLines (CRLF line endings, the blank line above
  the version line as the command wrote it). Nothing was replaced.
os: windows
args: []
