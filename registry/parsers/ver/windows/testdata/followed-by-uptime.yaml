source: the windows-server-2022 fixture followed by the procps-4.0.4 fixture of uptime/linux, joined for this case
description: a line after the version line is refused, since the output of ver is that one line and nothing after it
expect_error: does not describe this input
