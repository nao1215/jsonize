description: route -n prints the IPv4 table under its own banner, which this definition does not read.
source: the net-tools-numeric capture of route/linux, copied
expect_error: 'does not describe this input'
