source: captured on Ubuntu 26.04 (GNU coreutils 9.7) as who /var/log/wtmp with LC_ALL=C.UTF-8, eight of the lines; the user name and the link-local address replaced
description: records with a seat, a terminal, an X display, no line at all, and a remote host in the comment
os: linux
args: [/var/log/wtmp]
