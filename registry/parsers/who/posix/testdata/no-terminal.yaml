source: captured on Ubuntu 26.04 (uutils coreutils 0.8.0) with LC_ALL=C from the login records in /var/log/wtmp, the last 6 rows; the login name was replaced with alice
description: rows written without a terminal, so the line column is blank and no comment follows the time
os: linux
args: [/var/log/wtmp]
