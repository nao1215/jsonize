description: an editable install among the outdated packages, which adds the Editable project location column to the header, the rule and its own row
source: "`uv pip list --outdated` with uv 0.9.7 on CPython 3.12 (Ubuntu 26.04) with LC_ALL=C, in the same virtual environment after `uv pip install -e` of a local project; the path of the tree was replaced keeping its width"
os: linux
args: [list, --outdated]
