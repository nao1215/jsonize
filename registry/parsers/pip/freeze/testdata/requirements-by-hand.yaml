description: a requirements file written by hand, with a comment, a version range, an environment marker and an include, none of which pip freeze prints, and which the signature must refuse
source: written by hand to exercise the refusal the definition states for requirement lines that are not the three spellings pip freeze prints
expect_error: 'does not describe this input'
