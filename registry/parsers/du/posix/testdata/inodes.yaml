source: captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C; home directory path replaced. `du --inodes` counts inodes instead of blocks and prints the same two columns.
os: linux
args: [--inodes]
