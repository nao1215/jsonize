source: captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C; home directory path replaced. Every file is listed, so a path contains a space, an empty directory prints 0 and the sizes span three suffixes.
os: linux
args: [-ah]
