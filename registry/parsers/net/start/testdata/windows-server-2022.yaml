description: >-
  net start run for real on Windows Server 2022, the same layout as on
  2025 with fewer services running.
source: >-
  captured on GitHub Actions windows-2022 (Windows Server 2022, build
  20348.5499) on 2026-09-15 with `cmd /c "net start 2>&1"` from
  PowerShell, written with WriteAllLines (CRLF line endings, the blank
  lines around the list as the command wrote them). Nothing was
  replaced: the list names no user, host or address.
os: windows
args: [start]
