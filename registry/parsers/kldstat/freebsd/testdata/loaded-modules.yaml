source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C; if_bridge and nullfs were loaded for the capture
description: >-
  kldstat after loading if_bridge (which pulls in bridgestp) and nullfs.
  The size is hexadecimal without 0x and stays a string.
os: freebsd
args: []
