source: the first 30 lines of `rustup component list --toolchain 1.92` on Ubuntu 26.04 (rustup 1.29.1) with LC_ALL=C; the full listing is 141 lines
description: a toolchain that was installed without extra components, so only the first line carries the marker and the rest are false
os: linux
