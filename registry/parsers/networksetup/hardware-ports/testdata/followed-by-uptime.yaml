description: the uptime line joined after the VLAN heading is refused rather than read into the last port
source: the macos-26 fixture followed by the uptime/bsd fixture, joined for this case
os: darwin
args: [-listallhardwareports]
expect_error: does not describe this input
