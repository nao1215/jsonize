description: The secret keys with the long key ID after the algorithm, and ssb for the secret subkey.
source: "gpg -K --keyid-format long, GnuPG 2.4.8 on Ubuntu 26.04 with LC_ALL=C, in a GNUPGHOME made for the fixture with keys generated for it; the GNUPGHOME path replaced with /home/user/.gnupg and the rule shortened to match"
os: linux
args: [-K, --keyid-format, long]
