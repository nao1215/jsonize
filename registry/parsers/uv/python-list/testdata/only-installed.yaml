description: --only-installed leaves no download markers
source: captured with LC_ALL=C uv python list --only-installed on Linux (uv 0.9.7), output to a file; the user name in the home directory replaced
os: linux
args: [python, list, --only-installed]
