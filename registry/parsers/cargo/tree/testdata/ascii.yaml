description: The same project drawn with --charset ascii.
source: "captured with `cargo tree --charset ascii` (cargo 1.98.0) on Ubuntu 26.04 with LC_ALL=C; the project directory replaced with /home/user/demo"
os: linux
args: [tree, --charset, ascii]
