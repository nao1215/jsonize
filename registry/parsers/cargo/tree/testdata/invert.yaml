description: The packages that depend on memchr, where the root is a registry crate and the project with its path is a leaf.
source: "captured with `cargo tree --invert memchr` (cargo 1.98.0) on Ubuntu 26.04 with LC_ALL=C; the project directory replaced with /home/user/demo"
os: linux
args: [tree, --invert, memchr]
