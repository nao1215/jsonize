description: The features graph puts a feature where a package would be, which this definition does not read.
source: "captured with `cargo tree -e features` (cargo 1.98.0) on Ubuntu 26.04 with LC_ALL=C; the project directory replaced with /home/user/demo"
os: linux
args: [tree, -e, features]
expect_error: 'does not describe this input'
