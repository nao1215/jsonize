description: -t puts the time above the sample, which opens it
source: "`iostat -t --human nvme0n1 nvme1n1` on Ubuntu 26.04 (sysstat 12.7) with LC_ALL=C; the host name in the banner was replaced with a name of the same width"
os: linux
args: ["-t", "--human", "nvme0n1", "nvme1n1"]
