source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C
description: >-
  iostat -d prints a column group per device, named across the top, and
  no extended statistics. The text is refused.
os: freebsd
expect_error: 'does not describe this input'
