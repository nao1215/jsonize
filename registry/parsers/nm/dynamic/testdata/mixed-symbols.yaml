description: |
  A shared library, so both kinds of line appear: undefined and weak
  symbols with no value, and defined symbols with one. Lines 15 to 34 of
  the output, which is where the two kinds meet.
source: captured on Ubuntu 26.04 with GNU nm (GNU Binutils for Ubuntu) 2.46, LC_ALL=C nm -D /usr/lib/x86_64-linux-gnu/libz.so.1
os: linux
