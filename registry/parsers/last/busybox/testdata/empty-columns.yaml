description: one row whose TTY and HOST are both empty, which is what a login recorded without a terminal looks like
source: captured from BusyBox v1.37.0 (Ubuntu busybox package) with LC_ALL=C; the user name replaced with one of the same width
os: linux
