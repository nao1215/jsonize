source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C
description: last -s without -y. The login time has no year, and the text is refused.
os: freebsd
expect_error: 'does not describe this input'
