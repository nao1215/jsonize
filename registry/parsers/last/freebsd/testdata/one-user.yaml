source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C
description: last root lists that user's sessions only, with no boot row, and still ends with the utx.log line.
os: freebsd
args: ["root"]
