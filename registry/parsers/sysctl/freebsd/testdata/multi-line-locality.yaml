source: the vm.phys_locality variable as LC_ALL=C sysctl -a printed it on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm), lines 1772 to 1774 of that capture
description: >-
  vm.phys_locality prints an empty first line and the locality table on
  the next ("0: -1"), which read line by line would be a variable named 0.
os: freebsd
expect_error: 'does not describe this input'
