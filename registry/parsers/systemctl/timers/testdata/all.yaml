source: "systemctl list-timers --all --no-pager on Ubuntu with systemd 259, LC_ALL=C; the last two rows are timers with no next elapse, so all four time columns are \"-\""
os: linux
args: ["list-timers", "--all"]
