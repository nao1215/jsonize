source: captured on Ubuntu 26.04 (systemd 259) as systemctl show --property=ActiveState cron ssh with LC_ALL=C
description: two units print two blocks that do not say which unit each is, which is refused
os: linux
expect_error: "does not describe this input"
