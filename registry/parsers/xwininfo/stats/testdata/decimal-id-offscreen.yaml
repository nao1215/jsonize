source: captured on Ubuntu 26.04 (xwininfo 1.1.6, Xwayland under KDE Plasma) as xwininfo -id 0x400004 -int with LC_ALL=C
description: -int prints the window id in decimal; the window sits one pixel off screen, so the offsets are negative
os: linux
args: [-id, "0x400004", -int]
