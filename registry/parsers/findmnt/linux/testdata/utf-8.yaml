source: captured on Ubuntu 26.04 (util-linux 2.41.3) as findmnt with LC_ALL=C.UTF-8, the first twelve lines
description: in a UTF-8 locale findmnt draws the tree with box-drawing characters, which is what a pipe from an ordinary shell carries
os: linux
