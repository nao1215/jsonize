source: captured on Ubuntu 26.04 (util-linux 2.41.3) as findmnt -s with LC_ALL=C; the UUIDs and the user name in the last mount point replaced
description: the file systems /etc/fstab lists, where a swap file has no mount point and findmnt prints none
os: linux
args: [-s]
