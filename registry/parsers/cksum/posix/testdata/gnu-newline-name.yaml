source: captured on Ubuntu 26.04 with GNU coreutils 9.7 (gnucksum), LC_ALL=C cksum over a file made for the fixture
description: >
  A name with a newline, which GNU cksum prints as it is. The second
  half of the name is a line of its own and not a cksum line, so the
  text is refused.
os: linux
args: []
expect_error: does not match
