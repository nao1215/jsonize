description: brew list --versions prints a name and versions with no parentheses and belongs to brew/list-versions, so it is refused here
source: captured on GitHub Actions macos-latest (macOS 26.6.2) with LC_ALL=C brew list --versions
expect_error: does not describe this input
