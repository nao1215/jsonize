description: Two detached sessions started for the fixture; the heading and the count line end in a carriage return, which screen writes in its own tty mode.
source: captured on Ubuntu 26.04 with GNU screen 4.09.01, LC_ALL=C screen -ls with two sessions started by screen -dmS; the user name in the socket directory replaced
os: linux
args: [-ls]
