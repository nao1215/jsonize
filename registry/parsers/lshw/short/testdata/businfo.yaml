source: >-
  captured on Ubuntu 26.04 (lshw B.02.19) as an ordinary user with
  LC_ALL=C lshw -businfo -quiet -class memory -class processor
description: >-
  lshw -businfo prints the same table with bus addresses in the first
  column, which lshw/businfo reads and this definition does not.
os: linux
expect_error: 'does not describe this input'
