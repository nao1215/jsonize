description: Every namespace with -o wide, so SELECTOR follows AGE; the API server service has <none> there.
source: captured from kubectl v1.35.0 against a kind v1.35.0 cluster on Linux with LC_ALL=C
os: linux
args: [get, services, -A, -o, wide]
