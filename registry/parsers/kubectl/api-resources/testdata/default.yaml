description: Every resource the server serves; SHORTNAMES is empty for most rows and a comma list for some.
source: captured from kubectl v1.35.0 against a kind v1.35.0 cluster on Linux with LC_ALL=C
os: linux
args: [api-resources]
