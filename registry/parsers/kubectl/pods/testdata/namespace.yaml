description: One namespace, so the row opens with the pod name and there is no namespace key.
source: captured from kubectl v1.35.0 against a kind v1.35.0 cluster on Linux with LC_ALL=C
os: linux
args: [get, pods, -n, argocd]
