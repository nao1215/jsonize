description: Every namespace with -o wide, so CONTAINERS, IMAGES and SELECTOR follow AGE.
source: captured from kubectl v1.35.0 against a kind v1.35.0 cluster on Linux with LC_ALL=C
os: linux
args: [get, deployments, -A, -o, wide]
