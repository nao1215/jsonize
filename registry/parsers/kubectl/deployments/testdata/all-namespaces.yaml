description: Every namespace, so NAMESPACE leads the columns; coredns runs two replicas.
source: captured from kubectl v1.35.0 against a kind v1.35.0 cluster on Linux with LC_ALL=C
os: linux
args: [get, deployments, -A]
