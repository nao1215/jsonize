description: "`hostnamectl hostname` prints the name alone, with no label; it is not this format and must not be read as one."
source: captured on Ubuntu 26.04 (systemd 257) with LC_ALL=C; host name replaced
os: linux
args: ["hostname"]
expect_error: 'does not describe this input'
