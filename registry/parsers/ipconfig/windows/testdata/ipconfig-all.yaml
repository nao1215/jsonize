description: "`ipconfig /all` writes the machine's own settings under the banner before the first adapter block; it is the other variant's format and must not be read as this one."
source: >-
  the ipconfig /all output published at
  https://learn.microsoft.com/en-us/azure/virtual-network/ip-services/virtual-network-multiple-ip-addresses-portal
  (step 16 of the Windows section), prepared the way the valid fixture of
  ipconfig/all was: code block de-indented, prompt line dropped, host name
  and MAC address replaced with made-up values.
expect_error: 'does not describe this input'
