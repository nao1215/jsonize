description: >-
  ipconfig run for real on a Windows Server 2025 machine: the same two
  kinds of adapter as on 2022, printed in the same layout. CRLF line
  endings and the blank line above the banner are as the command wrote
  them.
source: >-
  captured on a GitHub-hosted windows-2025 runner (Windows Server 2025
  Datacenter, build 26100, en-US culture and UI language, console code
  page 65001) on 2026-09-13 with `ipconfig > ipconfig.txt` in cmd.exe.
  Replaced with made-up values of the same shape: the DNS suffix and the
  link-local IPv6 addresses. The private IPv4 addresses were kept.
os: windows
args: []
