source: captured on Ubuntu 26.04 (procps-ng 4.0.4) as uptime with LC_ALL=C
description: the report without -p also says up, and is uptime/linux's to read
os: linux
expect_error: "does not describe this input"
