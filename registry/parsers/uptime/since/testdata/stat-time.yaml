source: captured on Ubuntu 26.04 (GNU coreutils 9.7) as stat -c %y /etc/hostname with LC_ALL=C
description: a timestamp with fractions of a second and an offset is not what uptime -s prints
os: linux
expect_error: "does not describe this input"
