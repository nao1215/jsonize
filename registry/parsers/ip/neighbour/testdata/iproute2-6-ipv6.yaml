source: captured on Ubuntu 26.04 (iproute2 6.19.0) with LC_ALL=C
description: >-
  The first eight IPv6 entries, one of which is flagged router. The
  link-layer addresses were replaced, and the link-local addresses that
  encode one were recomputed from the replacement.
os: linux
args: ["-6", "neighbour"]
