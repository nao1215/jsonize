description: A free-text alias, which sits between the link layer and the alternative names and may hold spaces.
source: "not captured: the ip/link alias fixture (ip link on an Azure GitHub Actions ubuntu-latest runner, LC_ALL=C) joined the way ip -o joins a block, each line break replaced by a backslash, since no machine at hand carries an alias; the MAC address is replaced"
os: linux
