source: >-
  two lines of `ip -o link` captured on Ubuntu 26.04 (iproute2 6.19.0)
  with LC_ALL=C; the permanent address was replaced
description: >-
  A tunnel device reports the address it was built from after its
  broadcast address, which the device under it does not have.
os: linux
args: ["-o", "link"]
