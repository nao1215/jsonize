source: "ip route show table all on Ubuntu with iproute2 6.14, LC_ALL=C, one route of each type the local table prints. The main table prints no type at all, because every route in it is unicast; anycast appears only for IPv6. The addresses and the interface names are replaced."
os: linux
args: ["route", "show", "table", "all"]
