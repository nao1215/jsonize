source: "ip -s link on an Azure host, LC_ALL=C, as a GitHub Actions ubuntu-latest runner prints it (run 34155170184, 2026-09-07). The alias and the alternative name sit between the link line and the counter tables, which is why the tables are found by their header rather than by counting lines. The MAC address is replaced."
os: linux
args: ["-s", "link"]
