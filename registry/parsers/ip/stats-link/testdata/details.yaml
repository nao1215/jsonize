description: ip -s -d link adds the link details after the link address, which this does not read, so the text is refused.
source: ip -s -d link show lo, captured with LC_ALL=C on Ubuntu 26.04 (iproute2 6.19.0)
os: linux
expect_error: 'does not describe this input'
