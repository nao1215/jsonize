description: ip -s address adds the counter tables under each interface, which this does not read, so the text is refused.
source: ip -s address show lo, captured with LC_ALL=C on Ubuntu 26.04 (iproute2 6.19.0)
os: linux
expect_error: 'does not describe this input'
