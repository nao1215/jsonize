source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C and BLOCKSIZE=4k
description: df / with BLOCKSIZE=4k in the environment, which heads the column 4K-blocks and counts in 4 KiB blocks.
os: freebsd
args: ["/"]
