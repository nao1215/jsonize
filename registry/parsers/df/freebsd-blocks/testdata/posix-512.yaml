source: captured on FreeBSD 15.1-RELEASE-p3 (vmactions/freebsd-vm) with LC_ALL=C
description: df -P counts in 512-byte blocks, and the block size read from the heading is 512.
os: freebsd
args: ["-P"]
