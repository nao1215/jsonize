source: >-
  captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C; the rows
  for mount points under a user's home were left out
description: >-
  df -B512. The unit is spelled with its own suffix in the heading, and
  the same rule reads it.
os: linux
args: ["-B512"]
