source: captured on Ubuntu 26.04 (df (GNU coreutils) 9.7) with LC_ALL=C, tmpfs and efivarfs left out with -x and the user name in a mount point replaced
description: -BM puts the unit after every figure, and --total closes the table
os: linux
args: ["-P", "-BM", "-x", "tmpfs", "-x", "efivarfs", "--total"]
