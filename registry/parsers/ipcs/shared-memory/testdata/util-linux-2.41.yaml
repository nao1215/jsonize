source: |
  captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C; the owner
  name was replaced with one of the same width. The segment was created
  with ipcmk for the capture and removed again afterwards.
description: |
  One segment whose status flags are blank, so the row ends after nattch
  and status is null.
os: linux
args: [-m]
