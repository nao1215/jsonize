source: captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C
description: |
  The heading and nothing under it, which is what this machine prints
  when no shared memory segment exists.
os: linux
args: [-m]
