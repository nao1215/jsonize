source: |
  captured on Ubuntu 26.04 (util-linux 2.41.3) with LC_ALL=C; the owner
  name was replaced with one of the same width. The queue was created
  with ipcmk for the capture and removed again afterwards.
description: One queue, which is the smallest listing that has a row.
os: linux
args: [-q]
