description: The shortest build list this repository produces, where the main module line carries no version at all.
source: "`go list -m all` with go 1.26.4 on Ubuntu 26.04 with LC_ALL=C, inside this repository"
os: linux
