description: One binary built from this repository. The main module has no checksum of its own, so that cell is empty, and the build settings carry the vcs stamp.
source: "`go version -m` on a binary built with go 1.26.4 on Ubuntu 26.04 with LC_ALL=C; the binary path was replaced with /home/user/go/bin/jz"
os: linux
