description: The requirement graph of this repository, which includes the go and toolchain nodes the module system adds. No args are recorded because a landing check would run the command with an empty module cache, and loading the graph would then reach the network.
source: "`go mod graph` with go 1.26.4 on Ubuntu 26.04 with LC_ALL=C, inside this repository"
os: linux
