description: Mappings of a running process, including the ones this user may not stat, where the type is ERROR and the mount and inode cells are empty while the name is not.
source: the header and rows 1360 to 1366 of `lsfd` captured on Ubuntu 26.04 (util-linux 2.41) with LC_ALL=C; the user name was replaced
os: linux
