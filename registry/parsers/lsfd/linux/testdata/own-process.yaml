description: Every file of one shell, including the sockets and namespaces that have a description rather than a path.
source: captured on Ubuntu 26.04 (util-linux 2.41) with LC_ALL=C from `lsfd -p` of the capturing shell; the user name was replaced
os: linux
