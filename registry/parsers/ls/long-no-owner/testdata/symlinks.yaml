source: captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C from a directory under /tmp; the group name replaced with staff
description: a symbolic link and a name with a space in it, the same directory ls/long-no-group lists with -o
os: linux
args: ["-lg"]
