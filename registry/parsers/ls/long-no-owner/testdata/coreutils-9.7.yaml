source: captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C; nothing replaced, since dip, lp, shadow and root are group names the distribution creates
description: paths whose groups differ, so the one name column holds more than one value, with both date forms
os: linux
args: ["-lgd", "/etc/chatscripts", "/etc/cups", "/etc/gshadow", "/etc/hosts", "/etc/shadow"]
