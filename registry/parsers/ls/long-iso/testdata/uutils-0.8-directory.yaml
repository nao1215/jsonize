source: captured on Ubuntu 26.04 (uutils coreutils 0.8.0) as ls -l --time-style=long-iso internal in the jsonize repository with LC_ALL=C; the user and group names replaced
description: a directory listing with its total, the directories under it
os: linux
args: [-l, --time-style=long-iso, internal]
