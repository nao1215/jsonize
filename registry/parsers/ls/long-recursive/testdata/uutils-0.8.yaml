source: "uutils coreutils 0.8.0 (the ls installed as /usr/bin/ls) on Ubuntu 26.04, LC_ALL=C, ls -lR over the same tree as coreutils-9.7. The owner was replaced with a name of the same width"
os: linux
args: ["-lR", "tree"]
