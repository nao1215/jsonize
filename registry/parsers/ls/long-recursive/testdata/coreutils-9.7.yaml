source: "GNU coreutils 9.7 (gnuls) on Ubuntu 26.04, LC_ALL=C, ls -lR over a tree made for the fixture: a file whose name ends with a colon, one with a colon inside, one whose name holds \" -> \", a symbolic link and an empty directory. The owner was replaced with a name of the same width"
os: linux
args: ["-lR", "tree"]
