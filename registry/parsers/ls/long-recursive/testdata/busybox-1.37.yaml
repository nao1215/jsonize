source: "busybox 1.37.0 ls applet on Ubuntu 26.04, LC_ALL=C, ls -lR over the same tree as coreutils-9.7; BusyBox pads the columns wider. The owner was replaced with a name of the same width"
os: linux
args: ["-lR", "tree"]
