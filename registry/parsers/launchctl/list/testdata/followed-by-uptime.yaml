description: the uptime line joined after the jobs is refused rather than read as a job
source: the definition's own fixture followed by the uptime/bsd fixture, joined for this case
os: darwin
args: [list]
expect_error: line does not match pattern
