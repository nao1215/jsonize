description: A GPT image with no notes before the header, a name holding a space and a partition with no name.
source: sgdisk -p gpt.img on a 64 MiB image partitioned with sgdisk -n -t -c, GPT fdisk 1.0.10 on Ubuntu 26.04 with LC_ALL=C; disk GUID replaced
os: linux
args: [-p, gpt.img]
