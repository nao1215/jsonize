source: the same exiftool run with -G, which the groups variant reads
description: -G puts the group in front of every tag, so the default variant does not describe it
expect_error: 'does not describe this input'
