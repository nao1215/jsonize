description: "`whoami /groups` prints a table of the same layout under the GROUP INFORMATION banner; it is the groups variant's format and must not be read as this one."
source: >-
  the windows-server-2025 fixture of whoami/groups, captured on GitHub
  Actions windows-latest with `cmd /c "whoami /groups 2>&1"`.
expect_error: 'does not describe this input'
