description: -e, one block per device of a platform, headed "Device N on NAME platform:"
source: transcribed, not captured; the headings follow the snprintf "Device %d on %s" and printf "%s platform:" of src/egl/common/eglinfo.c in mesa-demos (main, 2026-09), and each block body is the GBM block of linux.txt. The mesa-utils 9.0.0 here has no -e, so this form is not verified on a real system.
os: linux
