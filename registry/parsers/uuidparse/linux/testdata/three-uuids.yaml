description: A random UUID with no time, a time-based one with a timestamp, and the nil UUID, whose type and time cells are both empty.
source: captured on Ubuntu 26.04 (util-linux 2.41) with LC_ALL=C over UUIDs made for this fixture; the hardware address the time-based UUID encodes was replaced
os: linux
