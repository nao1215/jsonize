source: the windows-server-2022 fixture followed by the windows-server-2022 fixture of netstat/windows, joined for this case
description: the connection table netstat prints after the last folder is refused rather than read as tasks of that folder
expect_error: 'field "status": required value is missing'
