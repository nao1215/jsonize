description: done with recoverable errors at two levels and a last update
source: printed on Ubuntu 26.04 with LC_ALL=C by print_status of cloud-init 26.1-0ubuntu3~26.04.1 (cloudinit/cmd/status.py, the function cloud-init status --long calls) for a StatusDetails built in Python; the messages are made up, the layout is the formatter's own
