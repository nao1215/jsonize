source: captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C. Three operands print three blocks, and a virtual filesystem reports no blocks and no inodes.
os: linux
args: [-f, /dev/shm, /proc, /sys]
