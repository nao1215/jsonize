source: "stat /dev/null on a GitHub Actions macOS 15.7.9 runner with LC_ALL=C: a character device, its device number, and a birth time of the epoch (capture run 34599785859 of the temporary capture workflow; the directory prefix of the names removed)"
os: darwin
args: ["/dev/null"]
