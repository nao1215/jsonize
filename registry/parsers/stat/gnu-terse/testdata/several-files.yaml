source: captured on Ubuntu 26.04 (GNU coreutils 9.7) with LC_ALL=C. A directory, a character device whose major and minor numbers are not zero, and a regular file.
os: linux
args: [-t, /etc, /dev/null, /etc/hosts]
