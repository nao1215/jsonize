description: >-
  tasklist /fo csv run for real on Windows Server 2025: every value
  quoted, memory figures with a comma inside the quotes, and image names
  printed whole where the table cuts them.
source: >-
  captured on GitHub Actions windows-latest (Windows Server 2025, build
  26100.33296) on 2026-09-15 with `cmd /c "tasklist /fo csv 2>&1"` from
  PowerShell, written with WriteAllLines (CRLF line endings). Nothing
  was replaced: the listing names no user, host or address.
os: windows
args: [/fo, csv]
