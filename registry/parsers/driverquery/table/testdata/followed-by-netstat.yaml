source: the windows-server-2022 fixture followed by the windows-server-2022 fixture of netstat/windows, joined for this case
description: the connection table netstat prints after the drivers is refused, since its lines name no driver type
expect_error: 'field "driver_type": required value is missing'
