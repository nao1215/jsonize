description: Rows about the process itself, where the thread id and thread name columns are empty.
source: lines 1 to 12 of `lsof -K -p 697` captured on Ubuntu 26.04 (lsof 4.99.4) with LC_ALL=C; every row names a path this user may not read, so nothing private is in it
os: linux
