source: captured with tokei 15.0.0 on Ubuntu 26.04 with LC_ALL=C NO_COLOR=1, tokei --files website/content in the jsonize repository
description: --files adds a row per file and a heading per file, which is refused
expect_error: 'does not describe this input'
