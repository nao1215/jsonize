description: One key type from the local sshd, whose comment line names the OpenSSH version. The key is the host key of a throwaway installation.
source: captured on Ubuntu 26.04 with OpenSSH 10.2p1, LC_ALL=C ssh-keyscan -t ed25519 localhost
os: linux
args: [-t, ed25519, localhost]
