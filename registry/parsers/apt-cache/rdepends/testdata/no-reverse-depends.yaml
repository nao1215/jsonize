description: Two packages nothing refers to, around one that has entries, so a heading with an empty list is followed by another package.
source: output of `apt-cache rdepends games-all sl ubuntu-gnome-desktop` captured on Ubuntu 26.04 (APT 3.2.0) with LC_ALL=C
os: linux
args: ["rdepends", "games-all", "sl", "ubuntu-gnome-desktop"]
