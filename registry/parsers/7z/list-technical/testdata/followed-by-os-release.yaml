source: the definition's own fixture followed by another definition's fixture, joined for this case
description: a line of another format after the listing, past the window the signature reads, is refused rather than read as a property
expect_error: expected "key = value"
